mongo {

  url {
    host = "localhost"
    url = ${?MONGO_HOST}

    port = "27017"
    port = ${?MONGO_PORT}
  }

  database = "sdkman"
  database = ${?MONGO_DATABASE}

  username = ""
  username = ${?MONGO_USERNAME}

  password = ""
  password = ${?MONGO_PASSWORD}
}